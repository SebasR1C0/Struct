#include <iostream>
#include <conio.h>
using namespace std;
void generar(char* v, int n) {
	for (int i = 0; i < n; i++) {
		v[i] = rand() % 26 + 'A';
	}
}
void imprimir(char* v, int n) {
	for (int i = 0; i < n; i++) {
		cout << v[i] << " ";
	}
	cout << endl;
}
bool upc(char* v, int n) {
	for (int i = 0; i < n-2; i++) {
		if (v[i] == 'U' && v[i + 1] == 'P' && v[i + 2] == 'C') { return true; }
	}
	return false;
}
bool sis(char* v, int n) {
	for (int i = 0; i < n - 4; i++) {
		if (v[i] == 'S' && v[i + 2] == 'I' && v[i + 4] == 'S') {return true; }
	}
	return false;
}
int main() {
	srand(time(NULL));
	int n;
	do {
		n = rand() % 11 + 20;
		char* v = new char[n];
		generar(v, n);
		imprimir(v, n);
		if (upc(v, n) || sis(v, n)) {
			if (upc(v, n)) {
				cout << "ES UN ALUMNO" << endl;
			}
			if (sis(v, n)) {
				cout << "ES UN ALUMNO DE SISTEMAS" << endl;
			}
			_getch();
		}
		cout << endl;
		delete[]v;
	} while (true);
	return 0;
}
